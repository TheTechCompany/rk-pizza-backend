const Resource = require("./resource")
const Pizza = require('./pizza')
const CalendarItem = require('./calendar-item')
const Model = require('./model')

module.exports = {
  Model,
  Resource,
  Pizza,
  CalendarItem
}
